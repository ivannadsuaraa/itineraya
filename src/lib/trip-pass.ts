// Pase de Viaje: pago único (mode: "payment", no suscripción) que desbloquea
// +1 itinerario completo (bonus_trips en profiles). Compartido entre cliente
// (pricing.tsx, new-trip.tsx) y servidor (webhook.ts) — no es un secreto.
export const TRIP_PASS_PRICE_ID = "price_1TqXRjClvzRH6emiUomH825r";
